RSpec.describe Catalog::Of::Things do
  it 'has a version number' do
    expect(Catalog::Of::Things::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
